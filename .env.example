# Subsonic-compatible music backend
# For streaming-provider albums (Spotify, etc.) use Octo Fiesta — it merges Navidrome + providers:
SUBSONIC_URL=http://host.docker.internal:5274
# Navidrome only (local library; no streaming-provider search):
# SUBSONIC_URL=http://host.docker.internal:4533
SUBSONIC_USERNAME=your_username
SUBSONIC_PASSWORD=your_password

# Mumble server (Murmur uses TLS on the control port by default)
# Melody in Docker, mumble-server container publishes 64738 on the host:
MUMBLE_HOST=host.docker.internal
MUMBLE_PORT=64738
# Alternative: join the mumble-server Docker network and use:
# MUMBLE_HOST=mumble-server

# Melody coordinator (sits in root, receives whispers)
MUMBLE_USERNAME=Melody
MUMBLE_PASSWORD=

# Optional client certificate for registered Mumble users (.pem format)
# MUMBLE_CERTFILE=/path/to/cert.pem
# MUMBLE_KEYFILE=/path/to/key.pem

# If Melody runs in Docker and Murmur is on the host OS (not in a container),
# use the host's LAN IP instead of localhost / 127.0.0.1

# MelodyPlayer pool (one connection per active channel)
# pool = fixed slots MelodyPlayer-1 .. MelodyPlayer-N
# per_channel = MelodyPlayer-{channel_name} per channel (good when no password required)
PLAYER_MODE=pool
PLAYER_POOL_SIZE=5
PLAYER_USERNAME_PREFIX=MelodyPlayer
PLAYER_PASSWORD=

# Accept commands typed in root channel (in addition to whispers to Melody)
COORDINATOR_ACCEPT_ROOT_MESSAGES=true

# Command prefixes (comma-separated, longest match wins)
COMMAND_PREFIXES=m/,melody/,/

# Channel behavior (seconds before player leaves an empty channel)
DISCONNECT_GRACE_PERIOD=300

# Initial playback volume for each channel session (0-100)
STARTING_VOLUME=100

# Low-latency playback tuning (raise probesize/analyzeduration if streams fail to start)
FFMPEG_PROBESIZE=32k
FFMPEG_ANALYZEDURATION=500k
PCM_TARGET_BUFFER_MS=80
PCM_MAX_PREBUFFER_FRAMES=6
PCM_PREBUFFER_BATCH_SIZE=1

# Search ranking for play/search (must sum to 100)
SEARCH_RELEVANCE_PERCENT=85
SEARCH_POPULARITY_PERCENT=15
SEARCH_RESULTS_LIMIT=10
LIST_WINDOW_SIZE=50

# Logging: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
